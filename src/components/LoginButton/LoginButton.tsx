import { Button } from "components/Button/Button";

export const LoginButton = () => {
    const handleClick = () => {
        //
    };
    return <Button label="ログイン" onClick={handleClick} />;
};
